<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.falle.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/animelogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/animelogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBarv21">
        <activity
            android:name=".InicioSesion"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AnimeInformation"
            android:label="AnimeK"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EpisodeInformation"
            android:label="AnimeK"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MangaInformation"
            android:label="AnimeK"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MangasSearch"
            android:label="Search Manga"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:label="AnimeK" />
        <activity android:name=".Register" />
        <activity android:name=".UserConfiguration" />
    </application>

</manifest>